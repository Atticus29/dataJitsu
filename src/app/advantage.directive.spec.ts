import { AdvantageDirective } from './advantage.directive';

describe('AdvantageDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvantageDirective();
    expect(directive).toBeTruthy();
  });
});
