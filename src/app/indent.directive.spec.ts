import { IndentDirective } from './indent.directive';

describe('IndentDirective', () => {
  it('should create an instance', () => {
    const directive = new IndentDirective();
    expect(directive).toBeTruthy();
  });
});
