import { WinDrawDirective } from './win-draw.directive';

describe('WinDrawDirective', () => {
  it('should create an instance', () => {
    const directive = new WinDrawDirective();
    expect(directive).toBeTruthy();
  });
});
